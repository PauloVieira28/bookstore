[project]
name = "bookstore"
version = "0.1.0"
description = "Bookstore API"
authors = [
    {name = "Paulo Vieira",email = "paulo_vieira22@hotmail.com"}
]
requires-python = ">=3.13"
dependencies = [
    "pytest (>=9.1.1,<10.0.0)",
    "django (>=6.1.1,<7.0.0)",
    "djangorestframework (>=3.18.1,<4.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
